from instagram import main as instagram_main
from chatAPI import main as chat_main

def main():
    # TODO we need to make the whole program more user friendly
        # Need to automate the install of packages such as pynput library
        # Also package python so the user does not need to install it using pyinstaller maybe
        # Copy userdata from users machine to our repo without having them copy it manually
        # Test this on linux and Mac OSX systems as well (especially when handling file uploads and directories)
    # instagram_main()
    chat_main()

main()